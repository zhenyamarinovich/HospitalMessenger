@model DoctorViewModel

<div>
    <h2>Все доктора.</h2>
</div>
<div class="container">
    <div class="row">
        @foreach (var doctor in Model.Doctors)
        {
         @Html.Partial("DoctorsSummary", doctor);
        }
    </div>
</div>

